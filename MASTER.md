# Veydria Cartography — Master Document

*Last updated 2026-05-08 · supersedes the scattered notes across `README.md`, `AGENTS.md`, and the HANDOFF series for high-level orientation.*

---

## 1. What this is

A two-output cartography system for the fictional continent of **Veydria**, driven by a single source-of-truth YAML file:

1. **Static parchment-style continental map** — generated by a Python pipeline (matplotlib + Voronoi + Shapely).
2. **Interactive web reference map** — Vite + React + Leaflet + D3, currently the primary product.

Both products consume the same `data/veydria-topology.yaml` so the static art and the interactive tool can never disagree. Worldbuilder is the upstream canonical repo; `scripts/sync-world-data.mjs` pulls from it.

The interactive map is intended as a **GM workbench** for tabletop sessions set in Veydria — not a player-facing atlas. The new player share mode (`#share=1`) is the controlled-export path, not the default.

## 2. The world (the "map idea" in one page)

Veydria is a roughly C-shaped continent wrapped around an enclosed sea, the **Aethelian Basin**. The basin is the continent's trade pivot — not a producer, but a neutral processor and certifier. Six port cities ring it, each with its own architectural tradition.

Outside the basin, the continent fragments into civilizations bounded by terrain: highland steppes north, oasis chains threading the southern desert, archipelagos west of the Halkar Straits. Trade routes the app surfaces (`Copper-for-Steel Road`, `Highland-Steppe Corridor`, `Coastal Monsoon`, `Caravan Thread`) are not generic D&D filler — each is a specific economic relationship that drives faction tension.

The interactive map exposes:
- ~3,050 features across 12 layer types
- 6 named civilizations
- Chokepoints (red), ports (yellow / red anchors), oases (green pins), sacred sites (blue / yellow stars), landmarks (white diamonds)
- Trade routes, rivers, faction overlay, terrain-cost heatmap

This is a worldbuilding artifact first; the cartography app is the lens through which a GM uses it.

## 3. Architecture

```
worldbuilder repo (canonical)
        │
        ▼  scripts/sync-world-data.mjs
data/                                          ← read-only here
├── veydria-topology.yaml      (3052 features)
├── coordinate-manifest.yaml
├── MAP-PROMPT.md
└── veydria-schematic.svg      (base parchment art)
        │
        ├──── generator/  (Python: pipeline.py)
        │     ├── core/        YAML → graph → Voronoi
        │     ├── render/      matplotlib parchment render
        │     └── export/      → output/veydria-spatial.geojson
        │
        └──── web/  (Vite + React 19 + Leaflet + D3 + vitest)
              ├── public/
              │   ├── veydria-schematic.svg     (overlaid on Leaflet)
              │   └── veydria-spatial.geojson   (3.2 MB, all features)
              └── src/
                  ├── App.tsx                    layer + share state, glue
                  ├── components/
                  │   ├── MapViewer              Leaflet + D3 overlay, pin drop
                  │   ├── InfoPanel              right-side feature card
                  │   ├── LayerControls          left-side layer + preset UI
                  │   ├── JourneyPlanner         routing + days + encounters
                  │   ├── SearchBar              Cmd-K palette
                  │   └── KeyboardHelp           ? overlay
                  └── utils/
                      ├── journey-graph          Dijkstra + civ-pivot fallback
                      ├── journey-days           per-day breakdown w/ weather
                      ├── encounters             seeded encounter rolls
                      ├── annotations            pins w/ feature linking
                      ├── layer-presets          6 built-ins + custom
                      ├── map-snapshot           PNG export ≤6 MP
                      ├── url-hash               #share=1 player mode
                      ├── measure                straight-line distance
                      └── travel-time            league/day math
```

### Data flow per interaction

- **Layer toggle** → `setLayers` → React effect → D3 overlay redraw (auto-recompute, no Recompute button needed).
- **Click feature** → `MapViewer` resolves nearest GeoJSON feature → `InfoPanel` opens with description, related features, linked notes.
- **Drop pin** → `findNearestFeature(x, y, features, 40svg)` auto-links if eligible → persisted to `localStorage:veydria-annotations-v2`.
- **Plan journey** → `findRouteWithFallback` (direct → 1-civ pivot → 2-civ pivot) → `buildDailyBreakdown` (deterministic weather, midpoint-bucketed encounters) → renders Route / Days / Encounters tabs.
- **Snapshot** → `html-to-image` rasterises the Leaflet container with all overlays baked in → clipboard or download, capped at ~6 MP.

## 4. Current feature inventory

| Area | Status | Notes |
|---|---|---|
| Layer system (12 types) | ✅ shipped | auto-recompute on toggle |
| Layer presets (6 built-in + custom) | ✅ shipped | localStorage `v1`, defensive merge |
| Search palette (Cmd-K) | ✅ shipped | fuzzy across all 3052 features |
| Info panel + related features | ✅ shipped | with linked-notes section |
| Journey planner | ✅ shipped | Dijkstra w/ civ-pivot fallback |
| Day-by-day breakdown | ✅ shipped | deterministic weather + encounters |
| Encounters tab | ✅ shipped | season-aware pools |
| Annotations / pins | ✅ shipped | auto-link to nearby feature |
| Player share mode (`#share=1`) | ✅ shipped | banner + chrome hide |
| PNG snapshot | ✅ shipped | clipboard-first, ≤6 MP cap |
| Measure tool | ✅ shipped | straight-line km/leagues |
| Tests | ✅ 38/38 | vitest, 5 files |
| CI | ✅ green | GH Actions, ~22s |

## 5. Roadmap

Three horizons, reorganised by state rather than time. Each item is sized **small** (a session) / **medium** (2–3 sessions) / **large** (multi-week).

### Shipped

Recent sessions (2026-05-08 → 2026-05-09) cleared the previous near-term list and added significant GM utility:

- **Hex grid overlay** — pointy-top odd-r grid (~110–600 cells via 30/50/70 size slider), A1/G7 labels, hover tooltip with terrain descriptors, click-to-select with `HexInfoPanel`, coord chip, deep-link via `#hex=G7`, variable size persisted to `localStorage`.
- **Hex-distance measurement** — two-click axial-distance mode, path highlighting with tinted endpoint/mid-path labels, "H" shortcut, URL persistence (`#hexA=G7&hexB=K12`), mobile fly-to gate.
- **Tactical preset** — built-in layer preset that pairs with hex distance (hex grid prominent, terrain dimmed).
- **Faction graph** — modal SVG view with civ nodes positioned by centroid, trade + shared-chokepoint edges, click → InfoPanel integration.
- **Random encounter roller** — "⟳ Roll one-off" in Encounters tab, gold-bordered Impromptu cards; severe trade-route beats now populated.
- **Mobile shape** — `100dvh` root, `invalidateSize` fixes, layer-controls collapsible pill, journey planner as bottom sheet, share-mode banner + Measure hide, 36px tap targets, feature fly-to with bottom-sheet padding.
- **Zoom polish** — `maxZoom: 1.5` on init fit, canvas padding 0.3, tooltip gated on mobile, `flyToBounds` with padding on feature/hex select.
- **Snapshot player variant** — Shift+click drops annotation pins before capture.
- **Bundle & UI polish** — code-split d3/leaflet/html-to-image into separate chunks, feature-count chip opens search palette, brighter player banner, annotation toolbar consolidated.

### In Progress / Next

Immediate gaps and plausible next-session moves, in rough priority order:

- **Manual mobile audit** *(small, recurring)* — real-device verification of all mobile paths: first-load fit, pinch-zoom smoothness, bottom-sheet contention, hex measure on phone, label tint at label-visible zoom. Checklist lives in `HANDOFF-2026-05-09c`.
- **Per-hex annotations** *(medium)* — optional `hexLabel` on `MapAnnotation`; pin UI shows "Linked to G7" instead of a feature name. Schema is additive; storage layer ready.
- **Variable-hexSize-aware route highlighting** *(medium)* — reuse `hexLineBetween` across consecutive journey nodes to highlight traversed hexes and add a "hexes traversed: N" stat. Recompute on hex size change.
- **"Roll one-off" defaults to current edge type** *(small)* — pre-select the edge type of the active journey segment instead of always defaulting.
- **Keyboard-shortcut cleanup** *(small)* — route `m`/`j`/`p` through the proper mutual-exclusion toggle handlers (same pattern as `h`) so they clear `hexMeasureMode` and URL state consistently.
- **Hex labels on the journey route** *(small)* — re-use the `data-label` hook to show "crosses G7 → H8 → I9 …" at a glance.
- **Snapshot URL** *(medium)* — compose a clipboardable link capturing `feature=` / `hex=` / `hexA=` / `hexB=` / `journeyFrom=` / `journeyTo=` / zoom + center.
- **Undo for Hex Measure** *(small)* — Backspace peels back the last endpoint (mirrors regular Measure mode).
- **Biome words + faction relationships** *(medium, upstream-blocked)* — requires `worldbuilder` edits (`biome` field on terrain_cell, `relationships:` block in topology YAML) followed by `npm run sync:data`. `hex-grid.ts` and `faction-graph.ts` are shape-ready.

### Backlog

Larger bets and exploratory directions:

- **Guided tour / onboarding** *(medium)* — first-visit walkthrough (5–7 steps) with backdrop-filter highlighting, `data-tour` attributes, and `localStorage` completion tracking. Detailed design sketch in §6.
- **Multi-route comparison** *(medium)* — overlay Direct vs Safest vs Cheapest routes simultaneously with distinct colours and side-by-side stat blocks.
- **Saved journeys** *(medium)* — persist journeys to `localStorage:veydria.journeys.v1`; list under "My journeys" in the planner.
- **Better Cmd-K** *(small)* — recent items, recently-linked annotation section, jump-to-civ shortcuts.
- **Time-of-day overlay** *(small)* — dawn/dusk/night tint slider for mood; affects nothing else.
- **Export markdown campaign log** *(medium)* — bundle active journey + dropped pins + visited features into a single session-log export.
- **Generative content per feature** *(large)* — Claude API "rumours, hooks, NPCs" panel per feature lore card. Cached per feature ID; optional, needs API key.
- **Dedicated mobile player mode** *(medium)* — share-mode URL renders cleanly on phone with no editing, just panning, info panels, and journey path. Potentially the killer at-the-table feature.
- **Time / calendar layer** *(medium)* — overlay civilizational calendar dates (festivals, monsoons, harvest) on the journey breakdown. Pulls from worldbuilder's calendar YAML.
- **Static map regeneration** *(large)* — "Render this view as parchment" button that hands layer state to the Python pipeline and produces a high-DPI PNG.

### Health / infrastructure (always-on)

- Keep CI green. Don't ship if `npm test` is red.
- Write tests alongside new state or schema. The 2026-05-07 audit added 7 retroactive tests; that gap shouldn't be the norm.
- Avoid `localStorage` schema breakage by versioning keys (`...v1` → `...v2`). Defensive merges on read are forward-compatible for additive changes; bump the key for breaking ones.

## 6. Guided tour — design sketch

A first-visit walkthrough is the right next big-ish feature once polish lands. Detail:

**Trigger.** Check `localStorage:veydria.tour.completed.v1`. If missing, on the first frame after `loaded === true`, fire the tour. Add a "Replay tour" entry to KeyboardHelp (?) for users who skipped.

**Mechanic.** A small floating card with text + a backdrop-filter dimming everything except the highlighted target (CSS `clip-path` or a mask-image + cutout). 5–7 steps, "Next" / "Skip" / "← Back" buttons, plus arrow keys.

**Steps (proposed):**

1. **Welcome — what this is.** Card centred. "Veydria is a fantasy continent. This map has 3,052 features. Let me show you the five things you'll actually use."
2. **Layers panel.** Highlight the layer column. "Toggle these on and off. Try a preset — `Politics view` is a good start." Demo: programmatically apply Politics preset on Next.
3. **Cmd-K search.** Highlight the Search button. "Anything in the world is two keystrokes away — try `Cmd-K` and type a city name." Open the palette pre-populated with `oravan`.
4. **Click → info panel.** Highlight the Aethelian Basin (programmatically open its InfoPanel). "Click any feature for its lore card. Notice the related features at the bottom — you can chain through them."
5. **Journey planner.** Highlight the Journey button, open it. "Pick two places — the planner finds a route, breaks it into days, and rolls weather and encounters per leg."
6. **Pins.** Highlight the Pin button. "Drop a pin near a feature and it auto-links. Use these for session notes."
7. **Player view.** Highlight the Share button. "Toggle this and your annotations and encounter notes hide. Send the URL to your players."
8. **Done.** "That's the tour. Press `?` for the keyboard help any time. Have fun."

**Implementation cost.** ~4–6 hours. Tour state is a tiny `useReducer` in `App.tsx`; each step is a `{ targetSelector, title, body, onEnter?, onLeave? }`. Total LOC ~200 + a CSS file.

**Test plan.** Vitest can verify the step graph (each step has a target selector that resolves in the DOM, no orphan steps, completion writes localStorage). The visual mask is manual.

**Risk.** Selectors break when components are renamed. Mitigation: tag each tour target with a stable `data-tour="layers"` attribute rather than relying on CSS classes.

## 7. Conventions

- **Memory keys.** All `localStorage` reads use a versioned key (`veydria.X.vN`). When in doubt, additive change → defensive merge; breaking change → bump version.
- **Tests next to code.** `foo.ts` → `foo.test.ts`. Vitest globs `src/**/*.test.{ts,tsx}`.
- **No HANDOFF without a state-of-the-tree note.** Each handoff opens with branch / CI / tests state so a future instance can orient in 30 seconds.
- **The data/ folder is read-only.** Edits go to worldbuilder, then `npm run sync:data`. (One known gotcha: `web/public/veydria-schematic.svg` is a manual mirror of `data/veydria-schematic.svg`; the sync script doesn't touch it. If the SVG ever shows mojibake again, copy it over — that's exactly what happened on 2026-05-08.)
- **Off-peak preference.** Heavy / long-running work happens evenings, mornings, weekends per the global Money Rule. Mid-session reasoning stays on Sonnet 4.6 with `opusplan` reserved for cross-cutting refactors.

## 8. Repo references

| File | Purpose |
|---|---|
| `README.md` | Short tech-stack intro, the README a new contributor reads first |
| `AGENTS.md` | Agent onboarding (what Kimi can/can't do, sync rules) |
| `MASTER.md` | This document — the project, the world, the roadmap |
| `HANDOFF-YYYY-MM-DD-*.md` | Session handoffs, ordered by date |
| `data/MAP-PROMPT.md` | The visual specification — what the map *should* look like |
| `data/veydria-topology.yaml` | The world (don't edit here; edit upstream) |
